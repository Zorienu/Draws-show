import { createMuiTheme } from "@material-ui/core/styles";
import { purple, cyan, red } from "@material-ui/core/colors";

const theme = createMuiTheme({
  palette: {
    primary: purple,
    secondary: red,
    terciary: red,
  },
});

export default theme;
